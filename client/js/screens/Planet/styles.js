import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  image: {
    // height: 100,
    // width: 50,
    resizeMode: "contain"
  },
  imageContainer: {
    padding: 25
  },
  background: {
    width: 380,
    height: 420
  }
});

export default styles;
